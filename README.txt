* To start the app,

- Build application
`npm install`

- Run application
`node index.js`

- Set PORT to 3000
`export PORT=3000` on Mac; `set PORT=3000` on windows


* Optional
- should use `nodemon` instead of `node` for development
`nodemon index.js` instead of `node index.js`
